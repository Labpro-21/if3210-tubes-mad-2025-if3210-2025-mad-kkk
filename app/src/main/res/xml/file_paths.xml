<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <cache-path name="images" path="." />

    <external-files-path
        name="external_files"
        path="." />

    <!-- Documents subdirectory in external files -->
    <external-files-path
        name="documents"
        path="Documents/" />

    <!-- Public Documents directory - ADD THIS -->
    <external-path
        name="public_documents"
        path="Documents/" />

    <!-- Root external storage - ADD THIS -->
    <external-path
        name="external_storage"
        path="." />

    <!-- Cache directory -->
<!--    <external-cache-path-->
<!--        name="external_cache"-->
<!--        path="." />-->

    <!-- Internal files directory -->
    <files-path
        name="internal_files"
        path="." />

    <!-- Downloads directory (if needed) -->
    <external-path
        name="external_downloads"
        path="Download/" />
</paths>
